import "../scss/style.scss";
import "./menu";
//import "./animation.js";

/* import debounce from "./debounce";
import { handleScroll, activateMenuItem } from "./scroll";


window.addEventListener("scroll", debounce(handleScroll, 50));
window.addEventListener("scroll", debounce(activateMenuItem, 50)); */
